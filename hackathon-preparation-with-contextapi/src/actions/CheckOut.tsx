
export default function CheckOut(customerInfo, cartData) {
        console.log(customerInfo)
        console.log(cartData)
        // create customer
        // create order
        // update inventory
}